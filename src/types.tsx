export interface User {
  id: number;
  username: string;
  email: string;
  firstName: string;
  lastName: string;
  isAdmin: boolean;
}

export interface Color {
  title: string;
  background: string;
  text: string;
  accent: string;
  highlight: string;
}
